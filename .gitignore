.DS_Store
mnt/.DS_Store
# Deployment state files — one per instance (.task-demo-state[.<instance>]).
# These hold live AWS resource IDs; never commit them.
deploy/.task-demo-state*

# Python
__pycache__/
*.pyc

# Local SQLite data and virtualenvs
*.db
.venv/
venv/
